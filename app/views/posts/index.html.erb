<% if @posts.any? %>
	<% @posts.each do |post| %>

		<h2><%= post.title %></h2>
		<p><%= post.description %></p>
		<h6><%= link_to "Edit post", edit_post_path(post.id) %></h6>
	<% end %>
<% else %>
	<h1>No posts yet :(</h1>
<% end %>